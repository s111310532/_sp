1. 銀行存提款實作分析

在不加鎖的情況下，Python 的 self.balance += 1 在位元組碼（Bytecode）層面實際上分為三個步驟：讀取變數、加 1、寫回變數。當兩個執行緒交錯執行時，就會發生更新被覆蓋的情況（Race Condition）。透過加入 threading.Lock()，我們將這段臨界區（Critical Section）保護起來，使其成為一個不可分割的原子操作 (Atomic Operation)，因此無論存提款多少次，最終餘額都絕對不會出錯。

2. 生產者消費者問題實作分析

本實作利用 Python 內建的 queue.Queue。該類別是執行緒安全的（Thread-safe），其內部使用了 threading.Condition 條件變數來控制同步：

當隊列達到 maxsize 時，put() 方法會呼叫 wait() 進入阻塞狀態，直到消費者呼叫 get() 釋放空間。

當隊列為空時，get() 方法會保持阻塞，直到生產者呼叫 put() 放入新數據。這種機制優雅地避免了忙碌等待（Busy Waiting），最大化 CPU 效率。

3. 哲學家用餐問題實作分析

如果每位哲學家都採取「先拿左側筷子，再拿右側筷子」的策略，當所有人同時飢餓時，每個人都會持有一支筷子並無限期等待下一支，這就滿足了死結的「循環等待」條件。
在本實作中，我們讓奇數號先拿左、偶數號先拿右。這樣相鄰的兩位哲學家將會優先競爭同一支筷子，其中一人必因無法取得第一支筷子而阻塞，且不會佔用另一支筷子，從根本上破壞了循環等待鏈，確保系統永遠不會發生死結。

