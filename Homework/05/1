1. 執行緒 (Thread)

執行緒是作業系統能夠進行運算排程的最小單位。它被包含在行程(Process)之中,是行程中的實際運作單
位。一個行程可以包含多個執行緒,這些執行緒共用該行程的記憶體空間(如全域變數、堆積空間 Heap),
但每個執行緒擁有自己獨立的程式計數器(Program Counter)、堆疊(Stack)以及暫存器群組。

2. 競態條件 (Race Condition)

當多個執行緒同時存取與操作同一塊共享數據,且最後的執行結果取決於執行緒執行的相對順序(排程隨機
性)時,即發生「競態條件」。此時,由於缺乏適當的同步機制,數據的一致性會遭到破壞。多個執行緒交錯
執行非原子性(Non-atomic)的操作是造成此現象的主因。

3. 互斥鎖 (Mutex / Mutual Exclusion Lock)

互斥鎖是一種用於多執行緒同步的機制,用來確保在任一時刻,只有一個執行緒可以進入臨界區(Critical
Section)存取共享資源。當一個執行緒取得互斥鎖後,其他企圖獲取該鎖的執行緒將會被阻塞(Blocked)或
等待,直到該鎖被釋放為止,藉此解決競態條件問題。

4. 死結 (Deadlock)
死結是指兩個或多個執行緒在執行過程中,因爭奪資源而造成的一種互相等待的僵持狀態,若無外力作用,它
們都將無法推進下去。死結的發生必須同時滿足以下四個必要條件(Coffman 條件):

互斥 (Mutual Exclusion):資源每次只能被一個執行緒使用。若有其他執行緒申請該資源,必須等待。

持有並等待 (Hold and Wait):執行緒已經保持了至少一個資源,但又提出了新的資源請求,而該資源已被其他執
行緒佔有。

不可剝奪 (No Preemption):執行緒已獲得的資源在未使用完之前,不能被其他執行緒強行奪走,只能由該執行
緒自行釋放。

循環等待 (Circular Wait):存在一個執行緒的資源等待鏈,例如 執行緒A 等待 執行緒B 持有的資源,執行緒B
等待 執行緒A 持有的資源。
